<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script>
    // Check browser support
    var lastUsedProductVersion;
    if ((typeof(Storage) !== "undefined") && (localStorage.getItem("{{ module }}_{{ productVersion }}_used_apiVersion"))) {
      lastUsedProductVersion =  localStorage.getItem("{{ module }}_{{ productVersion }}_used_apiVersion");  
    } else {
      lastUsedProductVersion = "{{ destination }}"
    }
    
    var request = new XMLHttpRequest();
    request.open("GET","./apiVersions.json", false);
    request.send(null);
    var jsonData = JSON.parse(request.responseText);
    lastUsedProductVersion = jsonData.apiVersions.includes(lastUsedProductVersion) ? lastUsedProductVersion : "{{ destination }}"
    window.location.href = window.location.href + lastUsedProductVersion;
  </script>
</head>
<body>
  
</body>

</html>